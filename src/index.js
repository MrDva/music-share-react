import React from "react";
import ReactDOM from "react-dom";
import AddTrancks from "./MusicList";
const rootElement = document.getElementById("root");

ReactDOM.render(
  <React.StrictMode>
    <AddTrancks/>
  </React.StrictMode>,
  rootElement
);
